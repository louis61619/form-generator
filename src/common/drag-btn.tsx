import Drag from '@iconify/icons-material-symbols/drag-handle';
import { Icon } from '@iconify/react';

export const DragIcon = ({ ...props }) => (
  <div {...props}>
    <Icon icon={Drag} />
  </div>
);
